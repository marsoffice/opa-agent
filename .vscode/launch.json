{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Attach to Functions",
      "request": "attach",
      "type": "coreclr",
      "processId": "${command:azureFunctions.pickProcess}"
    }
  ]
}
